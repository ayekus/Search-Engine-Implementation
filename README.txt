Aekus Singh Trehan - No partner
All steps for running the code follows the same steps as the automated tests
Just move any of the test files to the project directory and then run the file

Analysis of the code and project runtime and space complexity is in the analysis pdf

The crawler function accepts a string that must be an absolute URL, not a relative URL (crawler(seed))
The search function requires a search query (string of word(s)) and a True or False for boost - 
whether to multiply score by pagerank or not (search(phrase, boost))
